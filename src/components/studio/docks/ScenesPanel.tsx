"use client";
import { Copy, Minus, Plus, Settings2, ChevronDown, ChevronUp } from "lucide-react";
import { useStudioStore } from "@/store/studioStore";
import { Panel } from "./Panel";
export function ScenesPanel() { const project = useStudioStore((s) => s.project), add = useStudioStore((s) => s.addScene), remove = useStudioStore((s) => s.removeScene), select = useStudioStore((s) => s.selectScene), reorder = useStudioStore((s) => s.reorderScene); return <Panel title="Scenes"><div className="sf-list">{project.scenes.map((scene) => <button className={scene.id === project.selectedSceneId ? "selected" : ""} onClick={() => select(scene.id)} key={scene.id}><span>{scene.name}</span></button>)}</div><div className="sf-panel-tools"><button onClick={add} aria-label="Add scene"><Plus/></button><button onClick={() => remove(project.selectedSceneId)} aria-label="Remove scene"><Minus/></button><button onClick={add} aria-label="Duplicate scene"><Copy/></button><button aria-label="Scene settings"><Settings2/></button><span/><button onClick={() => reorder(project.selectedSceneId,-1)} aria-label="Move scene up"><ChevronUp/></button><button onClick={() => reorder(project.selectedSceneId,1)} aria-label="Move scene down"><ChevronDown/></button></div></Panel>; }
